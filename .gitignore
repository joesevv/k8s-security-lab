# Kubeconfig files
kubeconfig
*.kubeconfig
kind-kubeconfig*

# Environment files
.env
.env.*

# Private keys
*.key
*.pem
id_rsa*
cosign.key

# Sealed-secrets controller private key backups
sealed-secrets-key*.yaml
master-key*.yaml

# Unencrypted secret manifests
*-secret-plain.yaml
secrets/plain/

# OS junk
Thumbs.db
.DS_Store

# Editor dirs
.vscode/

# Backup / temp files
*.bak
*.tmp

# Local planning scratch notes — working docs, deliberately never committed
NEXT-SESSION-fable-brief.md

# Go build output. `go build ./...` drops the binary beside its source on
# Windows, so app/netpol-guard/netpol-guard.exe is one stray build away from
# being committed. The controller ships as source and a Dockerfile, never as
# a checked-in binary.
*.exe

